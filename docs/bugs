# when trying to manually tokenize quisque sed + space it forces sed ex into the generated token

# after tokenization loading POS tagging is loading forever. - [SOLVED]

# .i. is not being able to be tokenized - [SOLVED]

# repeating tokens related to token just after line break

# autotokenizer is duplicating century "y"

# (related to the above) Quotation marks (") cause text to be duplicated when auto tokenised.

# auto-tagger not working at all, and can't save manual POS tags.
